'use strict';

/* const number = 3;

function multiply(number) {
  let output = [];
  for (let i = 1; i <= number; i++) {
    let inner = [];
    for (let j = 1; j <= number; j++) {
      inner.push(i * j);
    }
    output.push(inner);
  }
  return output;
}
multiply(number);
 */
